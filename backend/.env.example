# Copy to .env and adjust as needed

# Requires MongoDB replica set for multi-document transactions.
# When using docker compose up, this is handled automatically.
MONGO_URI=mongodb://localhost:27017/logistics?replicaSet=rs0
PORT=4000
CORS_ORIGIN=*
